// src/hooks/useParallax.ts

import { useEffect } from "react";

export default function useParallax() {
  useEffect(() => {
    // Parallax logic will be added here if needed.
    // Leaving this empty keeps the hook safe to import.
  }, []);
}
